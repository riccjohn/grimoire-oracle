# Debug logs are written to ./oracle-debug.log by default in non-production environments.
# Override the path with DEBUG_FILE, or set NODE_ENV=production to disable logging.
# DEBUG_FILE=./oracle-debug.log
